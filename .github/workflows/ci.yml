---
name: CI

permissions:
  contents: read

on: # yamllint disable-line rule:truthy
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: "${{ github.head_ref || github.ref }}-${{ github.workflow }}"
  cancel-in-progress: true

jobs:
  lint-test:
    name: Lint and Test
    runs-on: ubuntu-22.04
    env:
      FIRESTORE_EMULATOR_HOST: localhost:9090
      CIR_API_BASE_URL: localhost:3030
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version-file: .python-version

      - name: Install Poetry
        run: python -m pip install poetry==2.4.1

      - name: Install Design System
        run: npm install

      - name: Install dependencies
        run: make install-dev

      - name: Lint Python
        run: make lint

      - name: Unit Tests
        run: make test-unit

      - name: Checkout eq-cir-fastapi
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          repository: "ONSdigital/eq-cir-fastapi"
          ref: "main"
          path: "eq-cir-fastapi"

      - name: Docker compose CIR
        run: |
          cd eq-cir-fastapi
          ls -ls
          docker compose up --build -d

      - name: Docker compose CIMS UI
        run: docker compose up --build -d

      - name: Functional Tests
        run: make test-functional

      - name: Docker compose CIMS UI shutdown
        run: docker compose kill

      - name: Docker compose CIR shutdown
        run: |
          cd eq-cir-fastapi
          docker compose kill
